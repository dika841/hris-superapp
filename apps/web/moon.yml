$schema: 'https://moonrepo.dev/schemas/project.json'

language: 'typescript'
stack: 'frontend'
dependsOn:
  - 'api'

tasks:
  dev:
    script: 'pnpm dev'
  build:
    script: 'pnpm build'
  check:
    script: 'pnpm check'
